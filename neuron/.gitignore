# Neuron-specific ignores (in addition to the repository-root .gitignore).

# Python virtual environments
.venv/
venv/

# Local secrets / environment files (NEVER commit these)
.env
**/.env
!**/.env.example

# Python build / cache artifacts
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local dev stack data (generated Synapse config, signing keys, DB volumes if bind-mounted)
deploy/compose/synapse-data/
deploy/compose/data/
